# Compliance benchmark pilot

No pilot results are included yet: this environment did not provide an authenticated agent CLI suitable for running `evals/compliance/run.mjs --exec --trials 2` across all fixtures. The instrument is committed so maintainers can run the pilot locally and replace this note with the generated report.
